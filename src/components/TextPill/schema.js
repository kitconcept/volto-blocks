export const TextBodySchema = (props) => ({
  title: 'Text block with background color',
  block: 'textPillWithStyle',
  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},
  required: [],
});
