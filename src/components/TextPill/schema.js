export const TextBodySchema = (props) => ({
  block: 'textPillWithStyle',
  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},
  required: [],
});
